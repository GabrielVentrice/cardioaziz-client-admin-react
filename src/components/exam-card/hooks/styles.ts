import styled from 'styled-components'

import { PopoverContent as ChakraPopoverContent } from '@chakra-ui/core'

export const PopoverContent = styled(ChakraPopoverContent)`
  position: absolute !important;
`
