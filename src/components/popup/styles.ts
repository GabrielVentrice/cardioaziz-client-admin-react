import styled from 'styled-components'

export const Container = styled.div`
  background: rgba(0, 0, 0, 0.1);
  position: fixed;
  height: 100vh;
  width: 100vw;
  z-index: 100;
  top: 0;
  left: 0;
`
